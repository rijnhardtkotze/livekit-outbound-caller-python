{
  "name": "livekit-outbound-caller-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for LiveKit outbound call dispatching with UI",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.10.2",
    "livekit-server-sdk": "^2.14.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.0.0"
  }
}
